# 改进说明（相对最初原版）

原项目路径：`e:\MyProject\yfbdocument\yfbdocument`  
本副本路径：`C:\Users\wangh\Desktop\yfbdocument-improved`

## 已做改进

1. **视觉**：主题色改为专业蓝；首页 Hero 与三栏文档入口卡片重做  
2. **导航/页脚**：去掉 Facebook Docusaurus、示例 Blog、ChatGPT/Deepseek 外链  
3. **关闭 Blog**，避免默认示例文章  
4. **搜索**：只保留 lunr 本地中文搜索；移除前端 Algolia 配置与冗余搜索依赖  
5. **文档样式**：正文字号、图片居中与阴影优化；修复 `style.css` 笔误  
6. **安全**：`pushToAlgolia.js` 改为环境变量，不再硬编码 Admin Key  
7. **脚本**：`generateDocsJSON.js` URL 与站点 `baseUrl` / `routeBasePath` 对齐  
8. **README**：改为本项目 npm 使用说明  
9. **图标**：蓝色文档 logo / favicon（替换默认绿恐龙）；`custom.css` 可调 Logo 大小位置  
10. **导出**：Word 保留标题层级与蓝/红文字颜色；PDF 用 html2canvas 程序生成（无浏览器页眉页脚网址）  

## 本地预览

```bash
cd C:\Users\wangh\Desktop\yfbdocument-improved
npm install
npm start
```

访问：http://localhost:3001/YFBDocument/

Logo 大小/位置：编辑 `src/css/custom.css` 顶部的 `--doc-logo-height` 等变量。
