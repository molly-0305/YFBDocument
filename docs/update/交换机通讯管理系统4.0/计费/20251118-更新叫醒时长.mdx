---
title: 20251118-更新叫醒时长
sidebar_position: 2
---
import React, { useState } from 'react';
import CodeBlock from '@theme/CodeBlock';
import '../../../../src/css/style.css';

修改要求：

1、更改叫醒时长（要求用户可以自己修改叫醒时长）
2、增加一键全选修改等级功能
3、将朗视改为Yeastar
4、增加叫醒失败弹屏


此版本下载链接：
<a href={require('@site/static/updateSoftware/Standalone/Billing/计费4.0 2025111801(更新叫醒时长）.rar').default} download="计费4.0 2025111801(更新叫醒时长）.rar">
  <p style={{color:"#2e8555"}}>计费4.0 2025111801（更新叫醒时长）</p>
</a>


### 🗄 数据库脚本

<MySqlAndSqlServer />

export function MySqlAndSqlServer() {
  const [showMySQL, setShowMySQL] = useState(false);
  const [showSQLServer, setShowSQLServer] = useState(false);

  const mysqlSql = `
INSERT INTO s_appconfig (itemKey,ItemValue) VALUES ('RingTimeout','60');
UPDATE d_defaultpbx SET Brand='Yeastar' WHERE Brand='朗视';
UPDATE d_specialcommunparam SET Brand='Yeastar' WHERE Brand='朗视';
`;

  const sqlserverSql = `
INSERT INTO s_appconfig (itemKey,ItemValue) VALUES ('RingTimeout','60')
GO
UPDATE d_defaultpbx SET Brand='Yeastar' WHERE Brand='朗视'
GO
UPDATE d_specialcommunparam SET Brand='Yeastar' WHERE Brand='朗视';
`;

  return (
    <div>
      {/* MySQL 按钮 */}
      <button
        style={{
          background: '#2e8555',
          color: '#fff',
          padding: '8px 14px',
          borderRadius: '6px',
          border: 'none',
          cursor: 'pointer',
          marginTop: '10px',
          marginRight: '10px'
        }}
        onClick={() => setShowMySQL(!showMySQL)}
      >
        {showMySQL ? '隐藏 MySQL 脚本' : '显示 MySQL 脚本'}
      </button>

      {showMySQL && (
        <CodeBlock language="sql">{mysqlSql}</CodeBlock>
      )}

      {/* SQLServer 按钮 */}
      <button
        style={{
          background: '#2e8555',
          color: '#fff',
          padding: '8px 14px',
          borderRadius: '6px',
          border: 'none',
          cursor: 'pointer',
          marginTop: '20px'
        }}
        onClick={() => setShowSQLServer(!showSQLServer)}
      >
        {showSQLServer ? '隐藏 SQLServer 脚本' : '显示 SQLServer 脚本'}
      </button>

      {showSQLServer && (
        <CodeBlock language="sql">{sqlserverSql}</CodeBlock>
      )}
    </div>
  );
}
