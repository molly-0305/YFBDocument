---
title: Windows 2019
sidebar-position: 7
---

import React from 'react';
import '../../../src/css/style.css'; // 导入 CSS 文件

### 1、服务器管理器
<p>
  点击左下角打开开始菜单找到服务器管理器菜单打开服务器管理器
</p> 
<img src={require('@site/static/img/softwareInstall/IIS/45.png').default} alt=""/>

### 2、添加角色和功能
<p>
  在弹出的服务器管理器界面找到添加角色和功能
</p> 
<img src={require('@site/static/img/softwareInstall/IIS/46.png').default} alt=""/>

<p>
  选择下一步
</p> 
<img src={require('@site/static/img/softwareInstall/IIS/47.png').default} alt=""/>

### 3、基于角色或基于功能
<p>
  选择：基于角色或基于功能的安装，然后下一步
</p> 
<img src={require('@site/static/img/softwareInstall/IIS/48.png').default} alt=""/>

### 4、服务器选择
<p>
  选择：从服务器池中选择服务器，然后下一步
</p> 
<img src={require('@site/static/img/softwareInstall/IIS/49.png').default} alt=""/>

### 5、服务器角色
<p>
  点击“下一步”
</p> 
<img src={require('@site/static/img/softwareInstall/IIS/50.png').default} alt=""/>

### 6、添加web所需功能
<p>
  选择：Web服务器（IIS），在弹出的对话框选择添加功能
</p> 
<img src={require('@site/static/img/softwareInstall/IIS/51.png').default} alt=""/>

<p>
  确保web服务器（IIS）选项有打钩，然后点击下一步
</p> 
<img src={require('@site/static/img/softwareInstall/IIS/52.png').default} alt=""/>

<p>
  勾选NET功能，继续点击下一步
</p> 
<img src={require('@site/static/img/softwareInstall/IIS/53.png').default} alt=""/>

<p>
  默认下一步
</p> 
<img src={require('@site/static/img/softwareInstall/IIS/54.png').default} alt=""/>

### 7、勾选功能
<p>
  (1)安全性勾选如下
</p> 
<img src={require('@site/static/img/softwareInstall/IIS/55.png').default} alt=""/>

<p>
  (2)常见HTTP功能勾选如下
</p> 
<img src={require('@site/static/img/softwareInstall/IIS/56.png').default} alt=""/>

<p>
  (3)性能勾选动态和静态压缩
</p> 
<img src={require('@site/static/img/softwareInstall/IIS/57.png').default} alt=""/>

<p>
  (4)运行状况和诊断勾选如下
</p> 
<img src={require('@site/static/img/softwareInstall/IIS/58.png').default} alt=""/>

<p>
  (5)应用程序开发可全部勾选也可以勾选需要的功能（建议全部勾选以免缺少后期需要还得重新来安装）
</p> 
<img src={require('@site/static/img/softwareInstall/IIS/59.png').default} alt=""/>

<p>
  (6)勾选FTP服务器（打开FTP功能）和管理工具如下，勾选完上述如下功能后继续点击下一步
</p> 
<img src={require('@site/static/img/softwareInstall/IIS/60.png').default} alt=""/>

### 8、确认安装功能
<p>
  确认安装以下功能，点击安装后等待安装进度完成
</p> 
<img src={require('@site/static/img/softwareInstall/IIS/61.png').default} alt=""/>

### 9、等待安装
<p>
  安装中，等待安装完成
</p>
<img src={require('@site/static/img/softwareInstall/IIS/62.png').default} alt=""/>


### 10、安装完成
<p>
  安装完成后点击关闭
</p> 
<img src={require('@site/static/img/softwareInstall/IIS/63.png').default} alt=""/>
